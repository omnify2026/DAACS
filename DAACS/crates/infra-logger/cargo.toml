[package]
name = "infra_logger"
version = "0.1.0"
edition = "2021"
description = "Tracing-based logging for DAACS Rust crates"

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
