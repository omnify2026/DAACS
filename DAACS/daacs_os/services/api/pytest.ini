[pytest]
minversion = 8.0
pythonpath = .
testpaths = tests
addopts =
    --strict-markers
    --strict-config
    -q
markers =
    integration: integration tests requiring external services
    contract: API contract tests
    performance: benchmark-like performance checks
